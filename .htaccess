# Block direct access to app/, config/, views/
RewriteEngine On

RewriteCond %{REQUEST_URI} ^/(app|config|views)/
RewriteRule ^ - [F,L]

# Block access to hidden files (e.g. .htaccess itself)
RewriteCond %{REQUEST_URI} ^/\.
RewriteRule ^ - [F,L]